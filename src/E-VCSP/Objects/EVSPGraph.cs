namespace E_VCSP.Objects
{
    internal class EVSPGraph
    {
        private Instance instance;
        public EVSPGraph(Instance instance)
        {
            this.instance = instance;

            // Using an instance, we create a discretized graph. 
        }
    }
}
